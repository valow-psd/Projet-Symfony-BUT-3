<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.css" rel="stylesheet"/>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
<nav class="bg-transparent border-gray-200 dark:bg-gray-900">
    <div class="max-w-screen-xl mx-auto flex flex-wrap items-center justify-between p-4">

        <a href="{{ path('home') }}" class="md:hidden flex items-center space-x-3 rtl:space-x-reverse">
            <span class="self-center logo-text text-2xl font-medium whitespace-nowrap dark:text-white">Freakyy</span>
        </a>

        <!-- Mobile menu button -->
        <button data-collapse-toggle="navbar-default" type="button"
                class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-gray-500 rounded-lg md:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600"
                aria-controls="navbar-default" aria-expanded="false">
            <span class="sr-only">Open main menu</span>
            <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M1 1h15M1 7h15M1 13h15"/>
            </svg>
        </button>

        <!-- Navbar items -->
        <div class="hidden w-full md:flex md:items-center md:justify-between" id="navbar-default">
            <!-- Logo -->
            <a href="{{ path('home') }}" class="hidden md:flex items-center space-x-3 rtl:space-x-reverse">
                <span class="self-center logo-text text-2xl font-medium whitespace-nowrap dark:text-white">Freakyy</span>
            </a>
            <div class="flex flex-col md:flex-row md:space-x-8 md:justify-center flex-grow">
                <ul class="font-medium flex flex-col md:flex-row md:space-x-8 rtl:space-x-reverse md:mt-0 bg-[#ffffee] dark:bg-gray-900">
                    <li class="flex items-center justify-center">
                        <a href="{{ path('home') }}"
                           class="block py-2 px-3 rounded {{ app.request.attributes.get('_route') == 'home' ? 'text-red-400' : 'text-[#382b45] hover:bg-gray-100 md:hover:bg-transparent md:hover:text-red-400 dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent' }}">Home</a>
                    </li>
                    <li class="flex items-center justify-center">
                        <a href="{{ path('event_list') }}"
                           class="block py-2 px-3 rounded {{ app.request.attributes.get('_route') == 'event_list' ? 'text-red-400' : 'text-[#382b45] hover:bg-gray-100 md:hover:bg-transparent md:hover:text-red-400 dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent' }}">Events</a>
                    </li>
                    <li class="flex items-center justify-center">
                        <a href="{{ path('event_new') }}"
                           class="block py-2 px-3 rounded {{ app.request.attributes.get('_route') == 'event_new' ? 'text-red-400' : 'text-[#382b45] hover:bg-gray-100 md:hover:bg-transparent md:hover:text-red-400 dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent' }}">Create
                            Event</a>
                    </li>
                    <li class="flex items-center justify-center">
                        <a href="{{ path('event_subscribed') }}"
                           class="block py-2 px-3 rounded {{ app.request.attributes.get('_route') == 'event_subscribed' ? 'text-red-400' : 'text-[#382b45] hover:bg-gray-100 md:hover:bg-transparent md:hover:text-red-400 dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent' }}">Subscribed</a>
                    </li>
                </ul>
            </div>

            <div class="flex items-center md:mx-auto justify-center">
                <ul class="font-medium flex flex-col md:flex-row md:space-x-8 rtl:space-x-reverse md:mt-0 bg-[#ffffee] dark:bg-gray-900">
                    {% if app.user %}
                        <li class="flex items-center justify-center">
                            <a href="{{ path('app_profile') }}"
                               class="block text-[#382b45] button-login hover:text-white font-medium text-sm px-5 py-2.5 me-2 mb-2">My
                                Profile</a>
                        </li>
                    {% else %}
                        <li class="flex items-center justify-center">
                            <a href="{{ path('app_login') }}"
                               class="block text-[#382b45] button-login hover:text-white font-medium text-sm px-5 py-2.5 me-2 mb-2">Login</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</nav>


<div class="p-10">
    {% block body %}{% endblock %}
</div>

<!-- Footer Section -->
<footer class="footer-bg py-10">
    <div class="max-w-screen-xl mx-auto px-4 md:px-8">
        <div class="flex flex-col md:flex-row justify-between items-center">

            <div class="grid grid-cols-1 md:grid-cols-4 gap-8 text-left md:text-left">
                <a href="{{ path('home') }}" class="mb-6 md:mb-0">
                    <h1 class="text-2xl logo-text-light font-medium">Freakyy</h1>
                </a>
                <div>
                    <h2 class="text-xl font-bold mb-2">About Us</h2>
                    <p class="text-sm">The best event planifier. Find the stunning and freakiest events near to you !</p>
                    <a href="#" class="underline text-sm">Read More</a>
                </div>
                <div>
                    <h2 class="text-xl font-bold mb-2">Our Service</h2>
                    <ul class="text-sm space-y-2">
                        <li><a href="#" class="hover:underline">Choose Event</a></li>
                        <li><a href="#" class="hover:underline">Ownership</a></li>
                        <li><a href="#" class="hover:underline">Contact</a></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="mt-10 text-center text-sm text-gray-400">
            <p>© 2024 Thomas Giraud & Valentin Munch. All rights reserved.</p>
        </div>
    </div>
</footer>

<script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.3.0/flowbite.min.js"></script>
<script>
    // JavaScript to handle the toggle
    document.addEventListener('DOMContentLoaded', function () {
        var toggleButton = document.querySelector('[data-collapse-toggle]');
        var menu = document.getElementById('navbar-default');

        toggleButton.addEventListener('click', function () {
            if (menu.classList.contains('hidden')) {
                menu.classList.remove('hidden');
            } else {
                menu.classList.add('hidden');
            }
        });
    });

    function toggleCollapsibleContent(element) {
        const content = element.querySelector('.collapsible-content');
        if (content.style.maxHeight) {
            content.style.maxHeight = null;
            element.classList.remove('border-2', 'border-red-500', 'expanded');
        } else {
            content.style.maxHeight = content.scrollHeight + "px";
            element.classList.add('border-2', 'border-red-500', 'expanded');
        }
    }
</script>
</body>
</html>
